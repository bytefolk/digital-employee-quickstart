# Issue snapshot

Fictional snapshot of the **acme-toolkit** issue tracker (last triage).

## Open issue count and themes (30 open issues)

1. Install / packaging (8 issues) — Windows install friction, PATH setup, native
   dependency downloads.
2. CLI behavior (9 issues) — JSON output stability, exit codes, `--no-color`
   deprecation confusion.
3. Documentation (6 issues) — missing migration notes, stale README examples.
4. Feature requests (7 issues) — plugin extension point, config file reload,
   tab completion.

## Duplicates and unassigned

- Duplicate pairs found: 3 (two in install, one in CLI behavior).
- Unassigned issues: 11.

## Suggestions (not decisions)

- Merge the 3 duplicate pairs before the next triage.
- Assign the 11 unassigned issues to maintainers or mark them "help wanted".
