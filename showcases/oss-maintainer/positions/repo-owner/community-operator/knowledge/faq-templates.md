# FAQ templates

Fictional FAQ reply templates for **acme-toolkit** (draft wording).

## Windows install

"Thank you for the report. The Windows install friction is tracked in the issue
snapshot; the `v2.4.0` plan includes Windows install improvements. For now, ensure
`acme-toolkit` is on PATH after installation."

## `--no-color` removal

"The `--no-color` flag was removed in `v2.3.0`. For `v2.4.0` migration, scripts
should stop passing it; see the migration notes in the release checklist."

## Plugin extension point

"The plugin extension point is planned for `v3.0.0`. This is a roadmap item, not a
shipped feature."

## Template rule

- Templates are drafts for review; do not claim they were posted or replied.
