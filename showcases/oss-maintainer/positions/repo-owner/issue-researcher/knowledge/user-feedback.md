# User feedback

Fictional user feedback digest for **acme-toolkit** (last two weeks).

- Top pain point: Windows install friction (mentioned in 12 threads).
- Second: JSON output stability and schema changes between minor versions.
- Third: migration from `v2.2.x` to `v2.3.x` — the removed `--no-color` flag confused
  several users.
- Positive signal: stable core CLI behavior appreciated; users ask for a plugin
  extension point.

## Research notes

- This digest is a snapshot for research only; it is not a product decision.
- To compare installation paths across repositories, only the data present in the
  snapshot may be used; anything missing is reported as unknown.
